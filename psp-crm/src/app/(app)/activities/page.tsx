import { Placeholder } from '@/components/Placeholder';
export default function Page() {
  return (
    <Placeholder
      title="Activities & tasks"
      milestone="Full build · milestone 6"
      blurb="Logging, reminders, and a “My Day” view over the activities/tasks tables (already created, empty in v0)."
    />
  );
}
