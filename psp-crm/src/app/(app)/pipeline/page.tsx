import { Placeholder } from '@/components/Placeholder';
export default function Page() {
  return (
    <Placeholder
      title="Pipeline"
      milestone="Full build · milestone 7"
      blurb="Kanban + table over the opportunities table (already created, empty in v0): create/edit, weighted amount, and coverage ratio."
    />
  );
}
